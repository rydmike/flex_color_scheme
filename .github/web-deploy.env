# GitHub Pages path slug for production example deploys.
# Change this when starting a new frozen minor/major (v9-0 -> v9-1).
# Loaded by deploy.yml and deploy_playground.yml after checkout.
WEB_VERSION=v9-0
